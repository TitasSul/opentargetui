{
  "name": "@opentargetui/extension",
  "version": "0.1.0",
  "private": true,
  "description": "OpenTarget UI browser extension.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@fontsource/geist": "^5.2.9",
    "@opentargetui/core": "workspace:*",
    "lucide-static": "^1.18.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
